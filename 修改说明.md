
# 添加自定义消息
需要修改 msg/CMakeLists.txt， 以及在 msg 目录下建立我们自定义的消息

# 添加驱动
在 src/drivers/drv_sensor.h 里添加 设备定义

boards/px4/raspberrypi/src/spi.cpp 里添加 新驱动

如果是 i2c 要在对应的 地方改

# 设置参数

SENS_IMU_MODE = 0
EKF2_MULTI_IMU = 3


# water main  driver

参考 src/drivers/imu/st/lsm9ds1/LSM9DS1.cpp  实现

bool WBotMainDriver::Reset() : 纯软件操作， 启动定时器

定时器每隔 N 秒执行  void WBotMainDriver::RunImpl()


# todo :

去 src/drivers/imu/st 下创建一个目录  lsm6dsv16x
参考 src/drivers/imu/st/lsm9ds1/ 写 kconfig

在 src/drivers/drv_sensor.h 里添加 设备定义

在 boards/px4/raspberrypi/default.px4board 添加 驱动 配置 =y

参考 src/drivers/imu/st/lsm9ds1/ 里的 .cpp .h 写 lsm6dsv16x 的驱动， 要 reset， whoami

